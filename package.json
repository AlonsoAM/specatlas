{
  "name": "specatlas-monorepo",
  "version": "0.0.0",
  "private": true,
  "description": "SpecAtlas — kernel determinista de Spec-Driven Development (monorepo)",
  "license": "MIT",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "pnpm -r build",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "pnpm -r typecheck",
    "docs:build": "tsx scripts/build-tutorial.ts",
    "satlas": "tsx packages/cli/src/bin.ts"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "tsup": "^8.3.5",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
